spring:
  application:
    name: service-netty

  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      # 配置中心设置
      config:
        namespace: chat
        group: dev
        file-extension: yaml

      # 服务发现设置
      discovery:
        namespace: chat
        group: dev
