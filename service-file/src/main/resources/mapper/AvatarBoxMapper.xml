<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zcj.servicefile.mapper.AvatarBoxMapper">

    <update id="decrementReferCount">
        UPDATE avatar_box
        SET refer_count = refer_count - 1,
            updated_at = #{currentTime}
        WHERE name = #{fileName}
          AND refer_count > 0
    </update>

    <update id="incrementReferCount">
        UPDATE avatar_box
        SET refer_count = refer_count + 1,
            updated_at = #{currentTime}
        WHERE name = #{fileName}
    </update>

</mapper>
